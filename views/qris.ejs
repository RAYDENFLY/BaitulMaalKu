<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 10px;
        }
        
        .qris-image {
            max-width: 100%;
            max-height: 100vh;
            width: auto;
            height: auto;
            object-fit: contain;
        }
        
        @media (max-width: 768px) {
            .qris-image {
                max-width: 95%;
                max-height: 90vh;
            }
        }
        
        @media (max-width: 480px) {
            .qris-image {
                max-width: 90%;
                max-height: 85vh;
            }
        }
    </style>
</head>
<body>
    <img src="<%= qrisImage %>" alt="QRIS BaitulMaal" class="qris-image">
</body>
</html>
